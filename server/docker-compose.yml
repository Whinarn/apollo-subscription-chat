version: "3.9"
services:
  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: dynamodb-local
    ports:
      - "8000:8000"
  redis:
    image: "redis:alpine"
    container_name: redis
    ports:
      - "6379:6379"
